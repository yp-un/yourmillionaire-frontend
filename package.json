{
	"name": "yourmillionaire-frontend",
	"private": true,
	"packageManager": "bun@1.3.12",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:landing": "bun --cwd apps/landing dev",
		"dev:dashboard": "bun --cwd apps/dashboard dev",
		"build": "bun run build:landing && bun run build:dashboard",
		"build:landing": "bun --cwd apps/landing build",
		"build:dashboard": "bun --cwd apps/dashboard build",
		"typecheck": "bun --cwd packages/ui typecheck && bun --cwd apps/landing typecheck && bun --cwd apps/dashboard typecheck"
	}
}
